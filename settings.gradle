pluginManagement {
  repositories {
    gradlePluginPortal()
  }
}

rootProject.name = 'Adaptive Performance Tweaks 1.20.1'
include("core")
include("gamerules", "items", "player", "spawn")
include("bundle")
