pluginManagement {
    repositories {
        gradlePluginPortal()
    }
}

rootProject.name = 'Adaptive Performance Tweaks'
include("core")
include("gamerules", "items", "player", "spawn")
include("bundle")
