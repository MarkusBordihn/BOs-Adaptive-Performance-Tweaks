pluginManagement {
  repositories {
    gradlePluginPortal()
  }
}

rootProject.name = 'Adaptive Performance Tweaks 1.19.2'
include("core")
include("gamerules", "items", "player", "spawn")
include("bundle")
