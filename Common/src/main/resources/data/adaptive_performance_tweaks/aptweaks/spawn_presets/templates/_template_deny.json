{
  "template": true,
  "notes": "TEMPLATE - Copy this file into config/adaptive_performance_tweaks/spawn_presets/, rename it without the leading _, then remove 'template: true'. Use deny_list to permanently block specific entities from spawning.",
  "replace": false,
  "mod_id": "your_mod_id",
  "priority": 200,
  "entities": {
    "deny_list": ["your_mod_id:entity_to_block"]
  }
}
